#include <stddef.h>

//=============================================================================
// copy S to D
//=============================================================================
char* strcpy (char *d, const char *s)
{
	char *tmp = d;

#if 0
	while ('\0' != *s)
		*d++ = *s++;

	*d = '\0';
#endif
	while ((*d++ = *s++) != '\0');

	return tmp;
}
